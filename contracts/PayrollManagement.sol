pragma solidity ^0.8.0;

contract PayrollManagement {

    function payAllEmployees() public {
        // for (uint i = 0; i < employees.length; i++) {
        //     payEmployee(employees[i].account);
        // }
    }
}